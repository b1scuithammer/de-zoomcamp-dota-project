# Copy this file to terraform.tfvars and fill in real values.
# terraform.tfvars is gitignored and must NEVER be committed.

project_id     = "your-gcp-project-id"
region         = "europe-west4"

kaggle_username = "your-kaggle-username"
kaggle_key      = "your-kaggle-api-key"
kaggle_dataset  = "username/dataset-name"

# Leave empty to use the built-in Cloud Build image from job/ directory
cloud_run_image = ""

gcs_prefix     = "raw/kaggle"
scheduler_cron = "0 2 * * *"  # Every day at 02:00 UTC

# Relative path to job source (default: ../job)
job_source_dir = "../job"
